# This directory is the local output target for daily journal files.
#
# After `npm run archive` completes, you will see one Markdown file per
# local date here (e.g. `2026-06-06.md`). The filename is the local-time
# date of the FIRST message in each archived task, never the file mtime
# of the source `.codex/sessions/*.jsonl` files.
#
# Before the first archive run, this directory is empty by design.
